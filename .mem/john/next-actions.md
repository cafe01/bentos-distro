# Next Actions (bentos_distro)

- [ ] M2.3: Verify module loading after first VM boot (cuse, vsock)
- [ ] M3: Full BentOS rootfs — Dart AOT binaries (bentosd, bentos-agent), containerd, user model
- [ ] M4: Build script automation — reproducible, pinned repos
- [ ] Consider making VIRTIO_VSOCKETS=y (built-in) in next kernel rebuild
